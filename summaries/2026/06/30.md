# Activity Summary for 6/30/2026

## 10:19:15 AM
The changes logged occurred on June 30, 2026, primarily between 9:20 AM and 10:16 AM, indicating a concentrated development session focused on front-end layout and styling adjustments within a PHP application.

**File: `c:\xampp\htdocs\NoticeBoard\view_post.php`**
This file, responsible for displaying individual posts, underwent numerous iterative changes related to its visual presentation:
*   **Initial Setup (9:20:14 AM):** The file fetched post details, associated images, and like counts using prepared statements. It rendered the post title, content, creation date (using `<h6>`), a like button, a horizontally scrollable image grid, and a comments section with a form to add new comments. Images were styled to be `100%` height and width, `display:inline-block;`.
*   **Image Grid Styling (9:20:43 AM - 9:37:55 AM):** The most frequent changes revolved around the image display within the "IMAGE GRID" section.
    *   Initially, the container for images used `overflow:auto; white-space:nowrap;` for horizontal scrolling. This was quickly refined to `overflow-x:auto;` (9:20:43 AM).
    *   The `display:inline-block;` style for images was toggled and eventually removed (9:21:33 AM, reverted 9:21:53 AM, then removed again 9:28:10 AM).
    *   A significant structural change occurred at 9:31:50 AM, where the single horizontal scroll container was removed. Instead, each image was wrapped in its own `div.mb-3` with `overflow-x:auto;`, and image styles were shifted to `max-width:none; height:auto;`. This suggests a change from a single scrolling strip to individual, potentially larger, horizontally scrollable images.
    *   Further micro-adjustments to image `max-width` and `height` properties were made several times (e.g., `max-width:auto;` at 9:34:53 AM, `height:100%;` at 9:35:52 AM, `max-width:100%;` at 9:36:18 AM), before settling back on `max-width:none; height:100%;` by 9:37:55 AM.
*   **Post Date Display & Comments Section Refinements (10:01:34 AM - 10:16:33 AM):**
    *   The post's creation date was moved from an `<h6>` tag to a `<small>` tag (10:01:34 AM). A temporary bug where it attempted to display a comment's creation date (`$c['created_at']`) instead of the post's (`$post['created_at']`) was introduced and quickly fixed (10:02:02 AM).
    *   The entire comments section was wrapped in a new `div` element (10:03:38 AM).
    *   This new `div` was then given a series of width-related Bootstrap utility classes (`w-md-50` at 10:14:23 AM) and then a custom CSS class `message-select` (10:15:08 AM).
    *   A dedicated `<style>` block was added to the document head to define the `message-select` class for responsive widths: initially `width: 100%;` and `width: 25%;` for medium screens (10:15:49 AM), which was subsequently adjusted to `width: 50%;` (10:16:14 AM) and finally `width: 60%;` (10:16:33 AM) for medium screens.

**File: `c:\xampp\htdocs\NoticeBoard\index.php`**
This file, likely the main listing page, also received styling updates, specifically for the category filter:
*   **Initial Setup (9:42:05 AM):** The file handled session management, included `config.php`, retrieved categories, and implemented an infinite scrolling mechanism for posts. It displayed a navbar, success alerts, a "New Post" button, and a category filter dropdown.
*   **Category Filter Styling (9:42:31 AM - 9:51:59 AM):**
    *   The `select` element for category filtering underwent multiple width adjustments using Bootstrap utility classes, moving from default to `w-50` (9:42:31 AM), then `w-40` (9:42:49 AM), and `w-25` (9:42:59 AM).
    *   A responsive class `w-100 w-md-25` was briefly introduced (9:46:43 AM) to make it full width on small screens and 25% on medium-and-up, but then reverted to a fixed `w-25` (9:48:19 AM).
    *   Finally, at 9:51:59 AM, a custom CSS class `.category-select` was introduced in a `<style>` block within the `<head>`, defining `width: 100%;` by default and `width: 25%;` for medium screens. The `select` element was then assigned this new custom class.

**Patterns and Recurring Elements:**
*   **Front-end Focus:** The vast majority of changes across both files involve HTML structure and CSS styling, particularly Bootstrap classes and inline styles, to control element widths, responsiveness, and overflow behavior.
*   **Iterative Styling:** There's a clear pattern of incremental adjustments to CSS properties (e.g., `width`, `height`, `max-width`, `overflow-x`) and the introduction/refinement of custom CSS classes (`.message-select`, `.category-select`) with responsive media queries.
*   **Consistent PHP Logic:** The underlying PHP logic for database interaction (fetching posts, images, comments, likes, categories) remained stable, primarily using prepared statements for security.
*   **Bootstrap Framework:** Bootstrap 5 is consistently used for styling and layout across both files.
*   **Security Practices:** `htmlspecialchars()` is consistently used when outputting dynamic data to the HTML, and prepared statements are utilized for database queries, indicating an awareness of common web security practices.

## 11:19:33 AM
**`c:\xampp\htdocs\NoticeBoard\view_post.php`**
This file, responsible for displaying individual posts, underwent continuous visual and structural refinements between 9:20 AM and 10:44 AM on June 30, 2026.
*   **Image Display:** Multiple changes were made to the styling of post images. Initially, images were within a single horizontally scrolling container. At 9:31 AM, this was refactored to wrap each image in its own `div` with `overflow-x:auto`, and various `max-width` and `height` properties were experimented with (e.g., `max-width:none; height:auto;` at 9:31 AM, then `max-width:auto; height:100%;` at 9:35 AM, then `max-width:100%; height:100%;` at 9:36 AM, and back to variations like `max-width:none; height:100%;` by 9:37 AM).
*   **Post Creation Timestamp:** The display of the post's `created_at` timestamp was adjusted. It moved from an `<h6>` tag to a `<small>` tag around 10:01 AM, and a variable reference error (using `$c` instead of `$post`) was corrected at 10:02 AM.
*   **Comments Section Layout:** The entire comments section was first encapsulated within a `<div>` at 10:03 AM. Subsequent changes focused on its responsiveness: applying Bootstrap's `w-md-50` class (10:14 AM), then a custom `.message-select` class with responsive CSS (10:15 AM - 10:16 AM, with width adjustments from 25% to 60% on medium screens). The final significant change at 10:26 AM involved replacing the custom width class with Bootstrap's grid system, placing the comments section within a `col-12 col-lg-7` div for better responsive control.

**`c:\xampp\htdocs\NoticeBoard\index.php`**
This file, serving as the main post listing page, was updated from 9:42 AM to 11:17 AM on June 30, 2026, with a focus on responsiveness, UI polishing, and the introduction of a new notification feature.
*   **Category Filter Responsiveness:** The `<select>` element for category filtering underwent several modifications to its width classes (e.g., `w-50`, `w-40`, `w-25`, `w-100 w-md-25`) before a custom CSS class `.category-select` was introduced at 9:51 AM to achieve responsive width (100% on small screens, 25% on medium and larger).
*   **Navbar Alignment:** Changes were made to how the navbar content (brand logo and new notification icon) was aligned. Classes like `justify-content-between` and `d-flex` were moved between the `container` and `nav` elements to achieve the desired spacing and alignment.
*   **Notifications Feature:** A significant new notification system was integrated starting at 10:57 AM. This involved:
    *   Adding a bell icon (`<i class="bi bi-bell"></i>`) with a badge (`<span id="notificationCount">`) to the navbar.
    *   Including the Bootstrap Icons CSS stylesheet at 11:07 AM.
    *   Adding JavaScript to the end of the body (11:04 AM) that periodically (every 5 seconds) calls a new backend script (`check_notifications.php`) to update the notification count.
    *   Implementing PHP code to store the maximum post ID in `$_SESSION['last_seen_post']` (11:14 AM - 11:17 AM) so the notification system can track newly added posts since the last visit.
*   **Success Message Fade Out:** A JavaScript `setTimeout` function for fading out success alerts after 3 seconds was clarified with a comment at 11:10 AM.

**`c:\xampp\htdocs\NoticeBoard\load_posts.php`**
This file, responsible for loading posts for infinite scrolling, saw a series of highly iterative changes to post card display between 10:46 AM and 10:53 AM on June 30, 2026.
*   **Post Details Presentation:** There was extensive experimentation with how the post title, truncated content, and especially the `created_at` timestamp were displayed:
    *   The `created_at` timestamp was initially displayed in a `<small>` tag, but then a duplicate `<h5>` tag was added, causing it to appear twice (10:46 AM). This was briefly followed by displaying it three times at 10:47 AM.
    *   The position and HTML tag (`small`, `h5`, `h6`) of the `created_at` timestamp, along with associated CSS classes (including a typo `ext-muted` corrected to `text-muted`), were frequently adjusted.
    *   Prefixes like "Title:", "MESSAGE:", "TEXT:", and "TIME:" were added and removed from the title, content snippet, and creation timestamp fields at various points (10:51 AM - 10:53 AM).
    *   Eventually, the duplicate `created_at` display was resolved, leaving a single `<h6>` tag for the timestamp by 10:53 AM.

**`c:\xampp\htdocs\NoticeBoard\check_notifications.php`**
*   **New Endpoint:** This file was introduced at 11:02 AM on June 30, 2026. It's a PHP script designed to serve the notification feature. It retrieves the `last_seen_post` ID from the session, queries the database for the count of posts with IDs greater than `last_seen_post`, and returns this count as a JSON object.

**Recurring Elements and Patterns:**
The development log exhibits a pattern of iterative refinement, particularly in frontend styling and layout using Bootstrap, often with minor adjustments and occasional temporary reverts occurring within minutes. SQL prepared statements are consistently used for database interactions across all PHP files, indicating a focus on security. A major new feature, a real-time notification system, was integrated across `index.php` and a new `check_notifications.php` endpoint. There was notable attention and multiple changes to the display format and placement of post creation timestamps.