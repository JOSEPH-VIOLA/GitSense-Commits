# Activity Summary for 3/30/2026

## 5:02:34 PM
The provided log details three C++ files, all timestamped around 3/30/2026, 4:07-4:08 PM, indicating they were likely part of a single development or submission session.

**File-Specific Updates:**

*   **c:\Intro to C++\Question3.cpp** (Timestamp: 3/30/2026, 4:07:43 PM): This file implements the 4th-order Runge-Kutta method to solve the differential equation `dc/dt = -k*c`, likely representing a decay process (e.g., concentration). It defines functions for the differential equation (`f`) and its exact solution (`exactValue`). The `main` function initializes parameters, including an initial concentration `C0 = 50.0` and a rate constant `k = 0.2`, then generates a table comparing the Runge-Kutta approximation with exact values at various times (t, 5 hours, 10 hours) over a 10-step simulation with a step size `h = 1.0`. A concluding comment highlights the use of numerical methods in biomedical engineering for dosage determination.

*   **c:\Intro to C++\Question2.cpp** (Timestamp: 3/30/2026, 4:07:59 PM): This program focuses on simulating radioactive decay using the 4th-order Runge-Kutta method for the differential equation `dy/dx = -c*y`. It interactively prompts the user for the half-life of the material, the total years of decay, and the desired time interval for printing results. It calculates the decay constant `c` from the user-provided half-life. The simulation starts with `y = 1.0` (representing 100% of the material) and `x = 0.0` (initial time), using a step size `h = 0.1`. It then prints a table comparing the Runge-Kutta approximation and the exact value at user-specified intervals throughout the decay period.

*   **c:\Intro to C++\Question1.cpp** (Timestamp: 3/30/2026, 4:08:07 PM): This file calculates the distance traveled by an object, considering initial velocity, mass, and air resistance. It defines global constants for initial velocity (`V0 = 6.0`), mass (`m = 25500000.0`), and resistance (`k = 44000.00`). The core calculation involves a differential equation `f(t, s)` representing `V0 * exp(-(k / m) * t)`. It features a dedicated `rungeKutta` function that computes the approximate distance `s` at a given time `tx` from initial conditions `t0`, `S0`, and a step size `h = 5.0`. An `exactDistance` function is also provided for comparison. The `main` function calculates and displays both Runge-Kutta approximated and exact distances at 10 seconds and 20 seconds.

**Patterns and Recurring Elements:**

All three files share several common characteristics:
*   **Numerical Method Implementation**: Each file primarily focuses on implementing the 4th-order Runge-Kutta numerical method to solve different types of ordinary differential equations (ODEs).
*   **Comparison with Exact Solutions**: For validation, each program includes a function (`exactValue` or `exactDistance`) to calculate the analytical solution of the respective differential equation, allowing for direct comparison with the numerical approximation.
*   **C++ Standard Libraries**: All files include `<iostream>` for input/output operations and `<cmath>` for mathematical functions (like `exp`, `log`).
*   **Namespace Usage**: They all use `using namespace std;`.
*   **Code Structure**: Each file defines a function `f` representing the differential equation `dy/dx = f(x, y)` and a `main` function orchestrating the simulation, calculations, and output.
*   **Student Identifiers**: A consistent multi-line comment appears at the beginning of each file, listing several student IDs and names (e.g., `BEEE/24/ME/029 - JOSEPH VIOLA`, `BEEE/23/ME/036 Steve Lunguzi`). This suggests these files are part of a group assignment or project.
*   **Timestamp Proximity**: The timestamps are very close (within a minute of each other on March 30, 2026), indicating they were likely saved or completed consecutively.